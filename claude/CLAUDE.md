Never use spaces around em dashes—use them like this, not like " — " this.

`~/.claude/settings.json` is managed by dotfiles. When making settings changes, ask whether to update `~/.dotfiles/claude/settings.json` instead.
